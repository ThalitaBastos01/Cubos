function solucao(lista) {
    //seu codigo aqui
   
    lista = [1,2,3,4];
    soma = lista.length + 1;
    
    for (let i = 0;i > lista.length;i++) {
        console.log(soma[i]);
    } 
    
}
solucao(soma);