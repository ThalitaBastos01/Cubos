// criando um array de 5 numeros
const numeros = [0, 2, 5, 7, 11];

//for (let i = 0; i < numeros.length; i++) {
//    console.log(numeros[i])
//}
let i = 0;
while (i < numeros.length) { //enquanto o i for menor que o array
    //executar isso
    console.log(numeros[i]);
    i++;
}